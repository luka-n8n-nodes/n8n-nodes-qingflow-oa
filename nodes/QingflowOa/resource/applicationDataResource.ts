import { ResourceOptions } from '../../help/type/IResource';

const ApplicationDataResource: ResourceOptions = {
	name: '数据内容 - 应用数据',
	value: 'applicationData',
	order: 50,
};

export default ApplicationDataResource;

