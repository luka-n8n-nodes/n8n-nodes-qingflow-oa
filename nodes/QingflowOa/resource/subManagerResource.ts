import { ResourceOptions } from '../../help/type/IResource';

const SubManagerResource: ResourceOptions = {
	name: '通讯录 - 子管理员',
	value: 'subManager',
	order: 30,
};

export default SubManagerResource;

